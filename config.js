const config = {
  db: {
    host: 'localhost',
    user: 'root',
    password: DB_PASSWORD,
    database: DB,
  },
  email: {
    username: USERNAME,
    password: PASSWORD,
    from: 'demo64241@gmail.com',
  },
};

module.exports = config;
